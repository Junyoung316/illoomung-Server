package com.reserve.illoomung.domain.repository;

import com.reserve.illoomung.domain.entity.StoreApprovalRequests;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StoreApprovalRequestsRepository extends JpaRepository<StoreApprovalRequests, Long> {
}
